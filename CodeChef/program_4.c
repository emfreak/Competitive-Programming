#include<stdio.h>
#include<stdlib.h>
int main(int argc, char *argv[])
{
             int n, fst = 0, sec = 1, fib, i,sum=0;
            n = atoi(argv[1]);
            while( a <= range )
            {
            sum += fst;
            fib = fst + sec;
            fst = sec;
            sec = fib;
            }
   printf("%d\n",fib);
   return 0;
}